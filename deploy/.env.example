# Fakturenn Docker Compose Configuration

# PostgreSQL
POSTGRES_DB=fakturenn
POSTGRES_USER=fakturenn
POSTGRES_PASSWORD=change_me_in_production

# Vault
VAULT_ROOT_TOKEN=root

# Paheko
PAHEKO_ADMIN_EMAIL=admin@younhasolena.bzh

# Logging
LOG_LEVEL=INFO

# Optional: Credentials (store in Vault in production)
# FREE_LOGIN=your_free_login
# FREE_PASSWORD=your_free_password
# FREE_MOBILE_LOGIN=your_free_mobile_login
# FREE_MOBILE_PASSWORD=your_free_mobile_password

# Optional: Path to credential files
# GMAIL_CREDENTIALS_PATH=/run/secrets/gmail_credentials
# SHEETS_CREDENTIALS_PATH=/run/secrets/sheets_credentials

# Optional: Application settings
# FROM_DATE=2024-01-01
# OUTPUT_DIR=factures
# HEADLESS_MODE=true
